<Window x:Class="StudentManagementApp.AssignSubjectsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Offered Subjects" Height="360" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleTextBlock" Text="Manage Offered Subjects" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>
        <TextBlock x:Name="InstructionTextBlock" Grid.Row="1" Text="Choose an action." Margin="0,0,0,8"/>
        <StackPanel Grid.Row="2">
            <Button Content="Offer a new subject" Height="38" Margin="0,4" Click="Offer_Click"/>
            <Button Content="Drop a subject" Height="38" Margin="0,4" Click="Drop_Click"/>
        </StackPanel>
        <Button Content="Back" Grid.Row="3" Height="38" Width="100" HorizontalAlignment="Right" Margin="0,15,0,0" Click="Back_Click"/>
    </Grid>
</Window>