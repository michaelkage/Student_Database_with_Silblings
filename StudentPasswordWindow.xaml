<Window x:Class="StudentManagementApp.StudentPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password" Height="250" Width="430" WindowStartupLocation="CenterOwner">
    <Grid Margin="25">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid.ColumnDefinitions><ColumnDefinition Width="170"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <TextBlock Text="Current password:" Grid.Row="0" Margin="5"/>
        <PasswordBox x:Name="CurrentPasswordBox" Grid.Row="0" Grid.Column="1" Margin="5"/>
        <TextBlock Text="New Password:" Grid.Row="1" Margin="5"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <Button Content="Save" Grid.Row="2" Grid.Column="1" Width="100" HorizontalAlignment="Right" Margin="5" Click="Save_Click"/>
    </Grid>
</Window>